# ContentApp

## CI/CD

This repository is currently operated in Azure-only CI/CD mode. GitHub Actions are intentionally paused and runnable workflow definitions are archived under `.github/workflows.disabled/`.

Start with:

- `azure-pipelines/README.md` for Azure pipeline creation and variable-group setup.
- `docs/CI_CD.md` for the current CI/CD operating model and re-enable policy.

